{
  "name": "@vampify/core",
  "version": "1.0.0",
  "description": "An opinionated Fastify framework featuring Drizzle-ORM and custom directory architecture.",
  "license": "ISC",
  "author": "babaliaris",
  "type": "module",
  "main": "./src/sandbox/server.ts",
  "scripts": {
    "lint": "eslint .",
    "build": "rimraf dist && tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "start": "cross-env NODE_ENV=production node dist/sandbox/server.js",
    "test:e2e": "cross-env NODE_ENV=test node --test --import tsx \"src/sandbox/tests/e2e/**/*.e2e.ts\"",
    "test:unit": "cross-env NODE_ENV=test node --test --import tsx \"src/sandbox/tests/unit/**/*.{test,spec}.ts\"",
    "dev": "cross-env NODE_ENV=development tsx watch src/sandbox/server.ts",
    "db:migrate": "npm run db:generate && npm run db:dev:migrate && npm run db:test:migrate",
    "db:generate": "cross-env NODE_ENV=development drizzle-kit generate",
    "db:dev:migrate": "cross-env NODE_ENV=development tsx src/sandbox/db/migrate.ts",
    "db:test:migrate": "cross-env NODE_ENV=test tsx src/sandbox/db/migrate.ts"
  },
  "dependencies": {
    "@fastify/autoload": "^6.3.1",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/env": "^5.0.3",
    "@fastify/jwt": "^10.0.0",
    "@fastify/sensible": "^6.0.4",
    "@fastify/swagger": "^9.7.0",
    "@fastify/swagger-ui": "^5.2.5",
    "@fastify/type-provider-typebox": "^6.1.0",
    "@sinclair/typebox": "^0.34.48",
    "bcrypt": "^6.0.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.4",
    "drizzle-orm": "^0.45.1",
    "fastify": "^5.7.4",
    "fastify-plugin": "^5.1.0",
    "mysql2": "^3.16.3",
    "pino-roll": "^4.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^25.2.1",
    "drizzle-kit": "^0.31.9",
    "eslint": "^10.0.1",
    "pino-pretty": "^13.1.3",
    "rimraf": "^6.1.2",
    "tsc-alias": "^1.8.16",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.0"
  },
  "overrides": {
    "bn.js": "5.2.3",
    "esbuild": "0.25.0",
    "minimatch": "10.2.3"
  }
}
